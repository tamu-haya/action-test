name: "Lock Issues, PRs, and Discussions0"
on:
  schedule:
    - cron: "0 0 * * *"
  workflow_dispatch:
    
jobs:
  lock:
    runs-on: "ubuntu-latest"
    steps:
      - uses: "dessant/lock-threads@v5"
        with:
          github-token: "${{ github.token }}"
          issue-inactive-days: "0"
          pr-inactive-days: "0"
          discussion-inactive-days: "0"
          
